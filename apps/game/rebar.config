{sub_dirs, ["rel"]}.

{deps_dir, "../../deps"}.

{erl_opts, [debug_info,
            fail_on_warning,
            warn_export_all,
            {parse_transform, lager_transform},
            {i, "../../include"}
            ]
}.
